import { MapPinIcon, PinIcon } from "lucide-react";

// MapPin.tsx
export default function MapPin({ active }: { active?: boolean }) {
    return (
        <MapPinIcon color="#0b111f" size={50} />
    );
}
